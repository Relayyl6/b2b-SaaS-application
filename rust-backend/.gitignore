/target
*/target

# Do NOT ignore Cargo.lock for binary workspaces — it ensures reproducible builds.
# Cargo.lock

# Per-service .env files (contain secrets, never commit)
.env
.env.example
/analytics/.env
/inventory-management/.env
/logistics/.env
/notifications/.env
/order-service/.env
/payments/.env
/product-catalog/.env
/supplier-management/.env
/user-management/.env